{
	"private": true,
	"scripts": {
		"ci": "yarn install --immutable --immutable-cache --check-cache",
		"build": "build(){ cd $1; slidev build --out ../docs/$1; }; build",
		"dev": "dev(){ cd $1; slidev --open; }; dev"
	},
	"dependencies": {
		"@slidev/cli": "^0.28.2",
		"@slidev/theme-default": "*",
		"@slidev/theme-seriph": "*"
	},
	"name": "classical-aesthetics"
}
